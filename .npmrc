# DSH's component packages declare peers for the complete product composition.
# This plugin installs only the direct packages its tests exercise; the target
# Harness supplies the rest at runtime, so do not resolve that transitive peer graph here.
legacy-peer-deps=true
